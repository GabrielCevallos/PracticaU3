package com.example.controller.tda.graph;

import com.example.models.Gimnasio;

public class TestTiempo {
    public static void main(String[] args) throws Exception {
        
        /* final Integer NUMERO_DATOS = 30;

        LabeledGraph<Gimnasio> labeledGraph = new LabeledGraph<>(NUMERO_DATOS, Gimnasio.class);

        for (int i = 1; i <= labeledGraph.numVertices(); i++) {
            labeledGraph.labelVertex(i, new GimnasioBuilder().mascotaWithGenericData().build());
        }

        labeledGraph.edgeAllVertices();

        Long t0 = System.currentTimeMillis();
        //labeledGraph.floydWarshall();
        labeledGraph.bellmanFord(1);
        Long t1 = System.currentTimeMillis();

        System.out.println("Total time: " + (t1 - t0) + "ms"); */
    }
}
